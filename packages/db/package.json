{
  "name": "@xclips/db",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@prisma/client": "^5.2.0"
  },
  "devDependencies": {
    "prisma": "^5.2.0",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init"
  }
} 